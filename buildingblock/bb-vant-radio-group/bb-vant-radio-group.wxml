<radio-group value="{{value}}" class="radio-group" bindchange="change">
  <label class="radio" wx:for="{{realFields}}" wx:key="{{index}}">
    <radio value="{{item.value}}" checked="{{item.checked}}"/>{{item.text}}
  </label>
</radio-group>